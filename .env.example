# Copy to .env and fill in. .env is gitignored — never commit a real key.
#
# The key is read by the API server (server/index.js) only. It is never sent to
# the browser and never appears in the client bundle.
ANTHROPIC_API_KEY=sk-ant-...

# Where the Blayne SKILL.md folders live, used only by `npm run skills:upload`.
# Not needed to run the app — the uploaded skill ids are committed in
# server/skills.json. Set this if you need to re-upload after editing a skill.
# SKILLS_DIR=C:\Users\<you>\AppData\Local\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\local-agent-mode-sessions\skills-plugin\<id>\<id>\skills

# Optional overrides
# BLAYNE_MODEL=claude-opus-5
# PORT=8787

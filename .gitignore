# dependencies
node_modules

# build output
dist
.vite

# secrets — never commit a real key
.env
.env.local
.env.*.local

# local tooling state (contains machine-specific absolute paths)
.claude/settings.local.json

# logs / OS noise
*.log
npm-debug.log*
.DS_Store
Thumbs.db
